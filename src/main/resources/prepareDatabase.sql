CREATE TABLE user (idUser int(10),
                    username VARCHAR(30),
                    active int(1),
                    role VARCHAR(10),
                    password varchar(20),
                    email VARCHAR(30)
);

CREATE TABLE topic (idTopic int(10),
                    title varchar(100),
                    )